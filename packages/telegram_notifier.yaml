telegram_bot:
  - platform: polling
    api_key: !secret tapikey
    allowed_chat_ids:
      - !secret tchatid


# Example configuration.yaml entry for the notifier
notify:
  - name: HomeNotifier
    platform: telegram
    chat_id: !secret tchatid